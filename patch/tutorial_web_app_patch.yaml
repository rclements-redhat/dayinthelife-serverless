spec:
  template:
    spec:
      containers:
          env:
            - name: WALKTHROUGH_LOCATIONS
              value: >-
                https://github.com/rclements-redhat/dayinthelife-serverless.git?walkthroughsFolder=/docs/labs
